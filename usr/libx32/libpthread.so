/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-x86-64)
GROUP ( /libx32/libpthread.so.0 /usr/libx32/libpthread_nonshared.a )
